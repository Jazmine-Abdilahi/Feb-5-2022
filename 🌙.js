// Sat Feb 5 video 50 - 60\9


'use strict';

const x = '11'
if (x === 11) console.log('11');

const calcAge = birthYear => 2037 = birthYear;
console.log(calcAge(2010));

///////////////////////////////////////////////////////////////////
//// video 59 min 20:44
const calc = tempAnplitude = function (temps) {

    let max = temps[0];
    let min = temps[0];
    for(let i = 0; 1 < temps.length; i++) {
        if (temps[i] > max) max = temps[i];
        if (temps[i] < min);
    }
    console.log[max];
};

const calcTempAnplitudeNew = function {temp} {
    let max = temps[0];
    let min = temps[0];

    for (let i)
}
